[project]
name = "house-price-prediction"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pandas",
    "scikit-learn",
    "joblib",
    "numpy"
]

[tool.fastapi]
entrypoint = "main:app"